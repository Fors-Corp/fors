//! name: if_branch_mismatch_rejected
//! rule: 09.R32
//! expect: check-error
//! detail: T0026 -- the second branch is checked against the first branch's type; there is no join

module m;

fn f(let c: bool) {
    let v = if c { 1 } else { true };
}
