//! name: linear-atomic-element-rejected
//! rule: 09.R11
//! expect: check-error
//! detail: T0011 -- ch01 R22b: `atomic[X]` with a linear `X` is ill-formed

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
}

struct Cell { slot: atomic[Res] }
