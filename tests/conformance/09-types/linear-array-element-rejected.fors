//! name: linear-array-element-rejected
//! rule: 09.R11
//! expect: check-error
//! detail: T0011 -- ch01 R22b: a CONCRETE `Array[X, N]` whose element type is linear is ill-formed; an element could leave it only by a partial move (ch01 R4a(c))

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
}

fn f() {
    var a: Array[Res, 2];
}
