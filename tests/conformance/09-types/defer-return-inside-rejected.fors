//! name: defer-return-inside-rejected
//! rule: 09.R33
//! expect: check-error
//! detail: T0033 -- ch01 R23c: no `return` inside a deferred body; the diagnostic names the enclosing `defer`

needs { };

fn f() -> i32 {
    defer { return 1; }
    return 0;
}
