//! name: std_module_without_use_mem_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: mem.Allocator without `use std.mem;` is N0014 at the head segment

module m;

fn f(let x: mem.Allocator) -> i32 { return 0; }
