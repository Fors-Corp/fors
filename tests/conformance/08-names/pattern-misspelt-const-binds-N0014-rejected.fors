//! name: pattern_misspelt_const_binds_N0014_rejected
//! rule: 08.R25
//! expect: check-error
//! detail: owner decision 2026-09-19 round 3 (D1) flips this test -- LIMTI is a bare one-segment pattern name and is now always a reference; it does not resolve, so it is N0014 (hint: to bind, write "let n"), not a fresh binding

module m;

const LIMIT: i32 = 10;

fn f(let n: i32) -> i32 {
    match n {
        LIMTI => LIMIT,
    }
}
