//! name: item_named_as_std_module_without_import_accepted
//! rule: 08.R13
//! expect: check-ok
//! detail: round 5 (D3) -- `io` is not a prelude name any more, so an item may be called `io` in a module that does not import std.io (flips round 2's item-named-as-prelude-module-rejected)

module m;

fn io() -> i32 { return 1; }
