//! name: std_module_without_use_proc_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: proc.Exec without `use std.proc;` is N0014 at the head segment

module m;

fn f(let x: proc.Exec) -> i32 { return 0; }
