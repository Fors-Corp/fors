//! name: duplicate_assoc_type_in_impl_rejected
//! rule: 08.R27
//! expect: check-error
//! detail: `type Item = ...;` twice in one impl

module m;

struct C { n: i64 }
impl Iterator for C {
    type Item = i64;
    type Item = i32;
    fn next(inout self: C) -> Option[i64] { return none; }
}
