//! name: std_module_without_use_gpu_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: gpu.Device without `use std.gpu;` is N0014 at the head segment

module m;

fn f(let x: gpu.Device) -> i32 { return 0; }
