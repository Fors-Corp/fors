//! name: path_ends_on_prelude_module_rejected
//! rule: 08.R16
//! expect: check-error
//! detail: a prelude module name is a module: a path that ends on it, outside use, is an error

module m;

fn f() -> i32 {
    let x = io;
    return 0;
}
