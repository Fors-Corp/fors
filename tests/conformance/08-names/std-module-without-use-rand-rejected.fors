//! name: std_module_without_use_rand_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: rand.Rng without `use std.rand;` is N0014 at the head segment

module m;

fn f(let x: rand.Rng) -> i32 { return 0; }
