//! name: import_collides_with_item_rejected
//! rule: 08.R13
//! expect: check-error
//! detail: the import binds `helper`, also an item declared later in main; reported at the use

module main;
use lib.helper;

fn helper() -> i32 { return 0; }
