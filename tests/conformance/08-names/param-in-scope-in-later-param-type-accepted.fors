//! name: param_in_scope_in_later_param_type_accepted
//! rule: 08.R26
//! expect: check-ok
//! detail: a parameter is in scope in the type annotations of the parameters after it, in the return type and in the body

module m;

fn f(let a: Arena, let x: Own[i32, a]) -> Own[i32, a] {
    return x;
}
