//! name: use_std_module_backed_by_source_accepted
//! rule: 08.R17
//! expect: check-ok
//! detail: round 5 (D3) -- once std ships as source the synthetic table is not consulted: `use std.io;` binds the real module std.io and io.Stdout resolves against its pub items; std.fs writes its own `use std.io;` like any other module

module main;
use std.io;

fn f(let x: io.Stdout) -> i32 { return 0; }
