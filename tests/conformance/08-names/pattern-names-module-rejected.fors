//! name: pattern_names_module_rejected
//! rule: 08.R16
//! expect: check-error
//! detail: round 3 (D1) -- a bare one-segment pattern is always a reference; one that resolves to a (prelude) module is a path that ends on a module, N0016

module m;

fn f(let v: i32) -> i32 {
    match v {
        io => 1,
        _ => 0,
    }
}
