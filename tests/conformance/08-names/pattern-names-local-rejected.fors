//! name: pattern_names_local_rejected
//! rule: 08.R25
//! expect: check-error
//! detail: owner decision 2026-09-19 round 3 (D1) -- a one-segment pattern that resolves to a local binding is N0025 directly (a pattern compares against compile-time entities only), not a fresh binding

module m;

fn f(let n: i32) -> i32 {
    let k: i32 = 1;
    match n {
        k => 1,
        _ => 0,
    }
}
