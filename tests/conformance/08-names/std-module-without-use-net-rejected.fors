//! name: std_module_without_use_net_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: net.Net without `use std.net;` is N0014 at the head segment

module m;

fn f(let x: net.Net) -> i32 { return 0; }
