//! name: fpat_shorthand_shadow_rejected
//! rule: 08.R25
//! expect: check-error
//! detail: the fpat shorthand x also binds x, which reuses the parameter x

module m;

struct P { pub x: i32 }

fn f(let p: P, let x: i32) -> i32 {
    match p {
        P { x } => x,
    }
}
