//! name: use_std_io_beside_prelude_accepted
//! rule: 08.R13
//! expect: check-ok
//! detail: use std.io binds io to the module the prelude's io already denotes

module m;
needs { io.stdout };
use std.io;

fn show(inout out: io.Stdout) raises io.Error { out.write_line("x")?; }
