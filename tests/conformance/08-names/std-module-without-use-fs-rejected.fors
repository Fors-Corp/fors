//! name: std_module_without_use_fs_rejected
//! rule: 08.R17
//! expect: check-error
//! detail: round 5 (D3) -- one test per known std module name: fs.Dir without `use std.fs;` is N0014 at the head segment

module m;

fn f(let x: fs.Dir) -> i32 { return 0; }
