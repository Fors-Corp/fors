//! name: contract-secret-rejected
//! rule: 02.R9
//! expect: check-error
//! detail: a `pre` contract expression mentions the `secret`-typed parameter `key`

needs { };

fn check(let key: secret i64)
    pre key > 0
{
}
