//! name: extern-c-no-raises
//! rule: 02.R13
//! expect: check-error
//! detail: an `extern "c"` function declares `raises`, which the C ABI boundary cannot carry

needs { };

enum Err { empty }

extern "c" fn compute(let n: i32) raises Err;
