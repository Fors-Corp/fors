//! name: contract-runtime-default
//! rule: 02.R9
//! expect: trap
//! detail: contract

module app.calc;
contracts: .runtime;

fn half(let n: i64) -> i64
    pre n >= 0
{
    return n / 2;
}

fn main(sink w: World) {
    let r: i64 = half(-4);
}
