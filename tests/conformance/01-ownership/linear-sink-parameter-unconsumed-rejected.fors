//! name: linear_sink_parameter_unconsumed_rejected
//! rule: 01.R22h
//! expect: check-error
//! detail: ch01 R22h -- a `sink` parameter of linear type carries the obligation; this diagnostic supersedes R4's

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
    fn poke(inout self) { }
    fn step(let self) raises Err { }
}
enum Err { boom }

fn f(sink r: Res) { }
