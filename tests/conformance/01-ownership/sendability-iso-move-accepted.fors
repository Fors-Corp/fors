//! name: sendability_iso_move_accepted
//! rule: 01.R13
//! expect: parse-ok
//! detail: n/a

needs { };

fn process(sink data: iso Buffer[u8]) {
    discard data;
}

fn spawn_work(sink data: iso Buffer[u8]) {
    parallel {
        spawn process(move data);
    }
}
