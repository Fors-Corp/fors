//! name: linear_local_dropped_at_return_rejected
//! rule: 01.R22h
//! expect: check-error
//! detail: ch01 R22h -- the `return` at 17:9 leaves the body with `r` live and unconsumed

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
    fn poke(inout self) { }
    fn step(let self) raises Err { }
}
enum Err { boom }

fn f(let n: i32) -> i32 {
    var r: Res = Res.open(1);
    if n == 0 {
        return 0;
    }
    r.close();
    return 1;
}
