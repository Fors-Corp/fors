//! name: linear_let_underscore_rejected
//! rule: 01.R22h
//! expect: check-error
//! detail: ch01 R22h -- `let _ = e;` drops a linear temporary

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
    fn poke(inout self) { }
    fn step(let self) raises Err { }
}
enum Err { boom }

fn f() {
    let _ = Res.open(1);
}
