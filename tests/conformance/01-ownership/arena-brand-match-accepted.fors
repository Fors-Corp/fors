//! name: arena_brand_match_accepted
//! rule: 01.R16
//! expect: parse-ok
//! detail: n/a

needs { };

struct Node[A: brand] { val: i64 }

fn use_it() {
    with arena one: Arena[Node[one]] {
        let x: Ref[Node[one], one] = one.alloc(Node { val: 1 });
        let v: i64 = one[x].val;
    }
}
