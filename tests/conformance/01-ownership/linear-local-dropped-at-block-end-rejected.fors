//! name: linear_local_dropped_at_block_end_rejected
//! rule: 01.R22h
//! expect: check-error
//! detail: ch01 R22h -- `r` of linear type `Res` is not consumed on the path leaving the body at its `}`; consume it with `Res.close`

needs { };

struct Res { fd: i32 }
impl Linear for Res { }
impl Res {
    fn open(let n: i32) -> Res { return Res { fd: n }; }
    fn close(sink self) { }
    fn poke(inout self) { }
    fn step(let self) raises Err { }
}
enum Err { boom }

fn f() {
    var r: Res = Res.open(1);
}
