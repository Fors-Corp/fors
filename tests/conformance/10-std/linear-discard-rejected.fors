//! name: linear_discard_rejected
//! rule: 10.S11
//! expect: check-error
//! detail: ch01 R22d -- neither `discard` nor `consume` satisfies a linear obligation

needs { };

fn f[A: brand, L: Allocator[A]](inout a: L) raises AllocError {
    var v: Vec[i32, A] = Vec.new();
    discard v;
}
