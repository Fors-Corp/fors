//! name: bitwise_needs_parens
//! rule: 07.D12
//! expect: parse-error
//! detail: "a + b & c" mixes an additive operand into bit_expr; bitwise operands are level-3 (cast_expr) only

fn f(let a: i32, let b: i32, let c: i32) {
    let r = a + b & c;
}
