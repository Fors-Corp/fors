//! name: contextual_keyword_invariant_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "invariant" is a keyword only in its one listed slot; used as an ordinary let-binding name it must parse

fn f() {
    let invariant = 1;
}
