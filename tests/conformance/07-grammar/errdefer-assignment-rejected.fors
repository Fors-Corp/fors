//! name: errdefer-assignment-rejected
//! rule: 07.D9
//! expect: parse-error
//! detail: as `defer-assignment-rejected`: `errdefer x = 1;` is a parse error at `=`

needs { };

fn a() {
    var x: i32 = 0;
    errdefer x = 1;
}
