//! name: contextual_keyword_soa_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "soa" is a keyword only in its one listed slot; used as an ordinary let-binding name it must parse

fn f() {
    let soa = 1;
}
