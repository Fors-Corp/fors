//! name: receiver_shorthand_both_forms
//! rule: 07.D21
//! expect: parse-ok
//! detail: round 5 (D1) -- `inout self` and `inout self: Self` are the same signature; the explicit form stays legal and stays tested

module m;

trait Bump {
    fn a(inout self) -> i64;
    fn b(inout self: Self) -> i64;
}
