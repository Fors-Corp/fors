//! name: reserved_keyword_break_as_identifier
//! rule: 07.Reserved
//! expect: parse-error
//! detail: "break" is reserved and never resolves as an identifier, so it cannot fill the binding slot of a let statement

fn f() {
    let break = 1;
}
