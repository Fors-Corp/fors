//! name: contextual_keyword_pre_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "pre" is a keyword only in its one listed slot; used as an ordinary let-binding name it must parse

fn f() {
    let pre = 1;
}
