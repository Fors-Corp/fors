//! name: reserved_keyword_inout_as_identifier
//! rule: 07.Reserved
//! expect: parse-error
//! detail: "inout" is reserved and never resolves as an identifier, so it cannot fill the binding slot of a let statement

fn f() {
    let inout = 1;
}
