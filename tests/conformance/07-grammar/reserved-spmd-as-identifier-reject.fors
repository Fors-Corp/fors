//! name: reserved_keyword_spmd_as_identifier
//! rule: 07.Reserved
//! expect: parse-error
//! detail: round 5 (D2) -- `spmd` is reserved (reserved-unused): it cannot fill a binding slot

module m;

fn f() {
    let spmd = 1;
}
