//! name: contextual_keyword_allocator_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "allocator" is a keyword only in its one listed slot; used as an ordinary let-binding name it must parse

fn f() {
    let allocator = 1;
}
