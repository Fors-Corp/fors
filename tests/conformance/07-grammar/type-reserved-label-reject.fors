//! name: type_reserved_label_reject
//! rule: 07.L1
//! expect: parse-error
//! detail: `type` cannot be a named-argument label

module m;

fn g(let n: i32) -> i32 { return n; }
fn f() -> i32 { return g(type: 1); }
