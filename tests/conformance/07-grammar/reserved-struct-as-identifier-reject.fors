//! name: reserved_keyword_struct_as_identifier
//! rule: 07.Reserved
//! expect: parse-error
//! detail: "struct" is reserved and never resolves as an identifier, so it cannot fill the binding slot of a let statement

fn f() {
    let struct = 1;
}
