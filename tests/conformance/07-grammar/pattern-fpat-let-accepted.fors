//! name: pattern_fpat_let_accepted
//! rule: 07.D17
//! expect: parse-ok
//! detail: the fpat form "let x" binds the field x (round 3, D1)

module m;

struct P { pub x: i32, pub y: i32 }

fn f(let p: P) -> i32 {
    match p {
        P { let x, y: let n } => x + n,
    }
}
