//! name: contextual_keyword_out_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "out" stays contextual (only an asm_item head); used as an ordinary let-binding name elsewhere it must parse

fn f() {
    let out = 1;
}
