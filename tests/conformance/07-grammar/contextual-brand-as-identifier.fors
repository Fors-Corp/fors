//! name: contextual_keyword_brand_as_identifier
//! rule: 07.D10
//! expect: parse-ok
//! detail: "brand" is a keyword only in its one listed slot; used as an ordinary let-binding name it must parse

fn f() {
    let brand = 1;
}
