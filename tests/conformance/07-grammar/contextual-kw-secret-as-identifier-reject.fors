//! name: contextual_kw_as_identifier
//! rule: 07.D10
//! expect: parse-error
//! detail: "secret" is a reserved qualifier keyword, not contextual, so it cannot be used as a binding name

fn f() {
    let secret = 1;
}
