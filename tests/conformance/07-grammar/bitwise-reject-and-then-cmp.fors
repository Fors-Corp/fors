//! name: bitwise_needs_parens
//! rule: 07.D12
//! expect: parse-error
//! detail: "a & b == c" mixes bit_expr and cmp_op; cmp_expr's two alternatives are exclusive, not chainable

fn f(let a: i32, let b: i32, let c: i32) {
    let r = a & b == c;
}
