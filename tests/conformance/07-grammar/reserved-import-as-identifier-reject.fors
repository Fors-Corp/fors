//! name: reserved_keyword_import_as_identifier
//! rule: 07.Keywords
//! expect: parse-error
//! detail: "import" is reserved for future use (no production), so it is never an identifier and cannot be a let-binding name

fn f() {
    let import = 1;
}
