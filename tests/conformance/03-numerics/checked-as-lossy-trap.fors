//! name: checked_as_rejects_lossy
//! rule: 03.R6
//! expect: trap
//! detail: checked-conversion  -- 300u32 as u8 is not exactly representable

module m;
needs { };

fn main(sink w: World) {
    var x: u32 = 300;
    var y: u8 = x as u8;
}
