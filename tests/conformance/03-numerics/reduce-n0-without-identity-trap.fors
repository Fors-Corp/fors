//! name: reduce_empty_without_identity_traps
//! rule: 03.R11a
//! expect: trap
//! detail: empty-reduce

module m;
needs { };

fn main() {
    var data: Array[f64, 0] = [];
    let xs: Slice[f64] = data[0 ..< 0];   // ch03 Rule 24
    var r: f64 = reduce(+, xs);
}
