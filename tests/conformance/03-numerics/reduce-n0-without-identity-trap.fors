//! name: reduce_empty_without_identity_traps
//! rule: 03.R11a
//! expect: trap
//! detail: empty-reduce

module m;
needs { };

fn main(sink w: World) {
    var xs: Slice[f64] = [];
    var r: f64 = reduce(+, xs);
}
