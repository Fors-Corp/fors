//! name: comptime_undeclared_file_read_rejected
//! rule: 04.R13
//! expect: check-error
//! detail: fs.read_to_string inside comptime with no @comptime_input(path) on the declaration must be a build error

module app;
needs { };
use std.fs;

fn setup() {
    comptime {
        let data: Str = fs.read_to_string(".config");
    }
}
