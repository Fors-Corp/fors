//! name: main_requires_sink_world
//! rule: 04.R8
//! expect: check-error
//! detail: fn main(let w: World) must be diagnosed: main's World parameter must use the sink convention

module app;
needs { };

fn main(let w: World) {
}
