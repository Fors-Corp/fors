//! name: needs_undeclared_capability_rejected
//! rule: 04.R1
//! expect: check-error
//! detail: main taking io.Stdout without declaring io.stdout in needs must be diagnosed

module app;
needs { };
use std.io;

fn main(inout out: io.Stdout) {
    out.write_line("ok");
}
