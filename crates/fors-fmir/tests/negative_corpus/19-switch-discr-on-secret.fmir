//! name: switch-discr-on-secret-rejected
//! expect: verify-reject
//! code: SecretBranchOrIndex
// ch05 Rule 6b: switch_discr's own rejection path (a match on secret).

decl 19 sig 1
block bb0
val %d = param 0 ty 1 secret ct=0
do switch_discr %d bb1 0:bb1
block bb1
do unreachable
