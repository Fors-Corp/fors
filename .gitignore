# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# cargo build output (the workspace's, and each standalone spike's)
/target/
/spikes/*/target/
/spikes/*/out/

__pycache__/

# Tooling smoke output: contaminated timings from a busy machine, never a result
# (ch06 rule 1). Regenerate with `calibrate.py --smoke`.
/bench/results/smoke/

# Local assistant tooling config
/.claude/
